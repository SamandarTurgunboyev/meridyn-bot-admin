import ReportsList from "@/features/reports/ui/ReportsList";
import SidebarLayout from "@/SidebarLayout";

const Reports = () => {
  return (
    <SidebarLayout>
      <ReportsList />
    </SidebarLayout>
  );
};

export default Reports;
