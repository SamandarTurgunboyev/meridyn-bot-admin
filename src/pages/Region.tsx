import RegionList from "@/features/region/ui/RegionList";
import SidebarLayout from "@/SidebarLayout";

const Region = () => {
  return (
    <SidebarLayout>
      <RegionList />
    </SidebarLayout>
  );
};

export default Region;
