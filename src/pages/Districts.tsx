import DistrictsList from "@/features/districts/ui/DistrictsList";
import SidebarLayout from "@/SidebarLayout";

const Districts = () => {
  return (
    <SidebarLayout>
      <DistrictsList />
    </SidebarLayout>
  );
};

export default Districts;
