import TourPlanList from "@/features/tour-plan/ui/TourPlanList";
import SidebarLayout from "@/SidebarLayout";

const TourPlan = () => {
  return (
    <SidebarLayout>
      <TourPlanList />
    </SidebarLayout>
  );
};

export default TourPlan;
