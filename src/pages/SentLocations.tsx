import LocationList from "@/features/location/ui/LocationList";
import SidebarLayout from "@/SidebarLayout";

const SentLocations = () => {
  return (
    <SidebarLayout>
      <LocationList />
    </SidebarLayout>
  );
};

export default SentLocations;
