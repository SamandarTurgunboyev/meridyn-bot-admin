import SpecificationsList from "@/features/specifications/ui/SpecificationsList";
import SidebarLayout from "@/SidebarLayout";

const Specifications = () => {
  return (
    <SidebarLayout>
      <SpecificationsList />
    </SidebarLayout>
  );
};

export default Specifications;
